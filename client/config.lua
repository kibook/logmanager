Config = {}

-- How often logs are uploaded to the server
Config.uploadInterval = 5000

-- Customize which client-side standard events to log
Config.events = {
	spawnmanager = {
		playerSpawned = true
	}
}
